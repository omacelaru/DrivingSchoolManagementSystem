spring:
  flyway:
    table: flyway_schema_history_vehicle_service

server:
  port: 8083

scheduling:
  service:
    url: ${SCHEDULING_SERVICE_URL:http://localhost:8082}

---

spring:
  config:
    activate:
      on-profile: "local-docker | container-docker"
scheduling:
  service:
    url: ""
